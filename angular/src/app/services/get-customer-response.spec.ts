import { GetCustomerResponse } from './get-customer-response';

describe('GetCustomerResponse', () => {
  it('should create an instance', () => {
    expect(new GetCustomerResponse()).toBeTruthy();
  });
});
